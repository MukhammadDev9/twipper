import type { FC } from "react";
import Box from "@mui/material/Box";

interface SearchBarProps {}

const SearchBar: FC<SearchBarProps> = ({}) => {
    return <Box bgcolor={"#bbb"}>Search Bar</Box>;
};

export default SearchBar;
