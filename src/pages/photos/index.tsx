import { FC } from "react";
import { PhotosPropsI } from "./types";
import { Box } from "@mui/material";

const Photos: FC<PhotosPropsI> = ({}) => {
    return <Box>Photos</Box>;
};

export default Photos;
